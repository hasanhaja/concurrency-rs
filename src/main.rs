use core::{generate_inputs, mult_process_images, Result, clear_outputs};

fn main() -> Result<()> {
    generate_inputs(15)
    // seq_process_images()
    // mult_process_images()
    // clear_outputs("seq-output-images")

    // clear_outputs("input-images")
}
